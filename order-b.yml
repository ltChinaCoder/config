spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/wecat_dinner?characterEncoding=utf-8
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      DEFAULT_ZONE: http://localhost:8761/eureka/
server:
  port: 8092